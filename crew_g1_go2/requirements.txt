# Install with:  /usr/bin/python3 -m pip install -r requirements.txt
# Do NOT use conda pip – it installs into Python 3.13, not the system 3.10 rclpy needs.
#
# numpy<2.0 is required: transforms3d (used by the autonomy stack's tf_transformations)
# uses np.maximum_sctype which was removed in NumPy 2.0.

crewai>=0.80.0
crewai-tools>=0.33.0
pydantic>=2.0.0
numpy<2.0
pyyaml
pytest
